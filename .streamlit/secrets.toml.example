# Copy this file to secrets.toml and fill in your real values.
# secrets.toml is gitignored. On Streamlit Community Cloud, paste these
# into the app's Settings -> Secrets panel instead.

ANTHROPIC_API_KEY = "sk-ant-..."

# Optional. If set, the app shows a password prompt before the chat.
# Leave commented out for open access.
# DEMO_PASSWORD = "change-me"
